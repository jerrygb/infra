{
  "rabbitmq": [{
    "host": "{{ rabbitmq.host }}",
    "port": {{ rabbitmq.port}},
    "user": "{{ rabbitmq.user }}",
    "password": "{{ rabbitmq.password }}",
    "vhost": "{{ rabbitmq.vhost }}"
  }]
}
